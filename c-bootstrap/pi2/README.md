# DebugC
A very small and simple C-language bootstrapper which does the same thing as the machine code bootstrapper.

Intended to be a simpler way of understanding the paths the machine code versions take and how they work.
